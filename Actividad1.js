// Actividad 1

// Inicio un ciclo hasta 20
for (let i = 1; i <= 20; i++) {
    // Condición que valida el residuo
    if ((i % 3) == 0) {
        // Si es TRUE mostramos el número
        console.log(`Número: ${i}`)
    }
}